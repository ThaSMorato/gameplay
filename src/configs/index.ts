import { COLLECTION_APPOINTMENT, COLLECTION_USER  } from './storage';

import {
    REDIRECT_URI,
    SCOPE,
    RESPONSE_TYPE,
    CLIENT_ID,
    CDN_IMAGE
} from './discordAuth';


export {
    COLLECTION_APPOINTMENT,
    COLLECTION_USER,
    REDIRECT_URI,
    SCOPE,
    RESPONSE_TYPE,
    CLIENT_ID,
    CDN_IMAGE
}